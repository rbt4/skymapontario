// Optional Mapbox basemap support.
// Add a URL-restricted public token to use Mapbox Dark on the landing page.
// Leave empty to use the built-in CARTO/OpenStreetMap fallback.
window.SKYMAP_MAPBOX_PUBLIC_TOKEN = '';
