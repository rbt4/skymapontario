name: Legacy SkyMap build (manual only)

on:
  workflow_dispatch:

jobs:
  legacy-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy pipeline disabled
        run: |
          echo "This obsolete SkyMap 8 pipeline is intentionally disabled."
          echo "Use the Build SkyMap Ontario 10 Local Intelligence workflow instead."
