# SkyMap Ontario 12 Pages validation

This temporary branch exists only to run the aligned main-branch Pages workflow in pull-request mode. It validates the verified product-site overlay, reconstructed SkyMap 12 web app, matching Android APK and Pages artifact together.

Validation rerun after aligning the main-branch AndroidX setting with SkyMap 12.
